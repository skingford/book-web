# book-web

## git

```bash
echo "# book-web" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/skingford/book-web.git
git push -u origin main
```

or
```bash
git remote add origin https://github.com/skingford/book-web.git
git branch -M main
git push -u origin main
```
